<Logger>
<!-- valid replacements are 
     %ServerRoot% - MiServer root directory
     %SiteRoot%   - web site root -->
  <active>1</active>                      <!-- 1 for yes, 0 for no -->
  <anonymousIPs>0</anonymousIPs>          <!-- 1 for yes (GDPR compliant), 0 for no -->
  <directory>%SiteRoot%/Logs/</directory>  <!-- directory to store log files -->
  <interval>30</interval>                 <!-- cache write time interval in seconds, 0 means write immediately (do not cache) -->
  <prefix></prefix>                       <!-- character vector log file prefix -->
</Logger>